# Crossref Work Lookup for runx

Governed, read-only Crossref connector skill. The package declares a Nango
connector dependency, typed inputs and output, minimal scope, explicit policy,
named emit, and deterministic fixtures.

Run verification with:

```sh
runx tool build --all --json
runx doctor --json
runx harness . --json
```

